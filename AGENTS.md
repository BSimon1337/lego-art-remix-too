# lego-art-remix-too Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-09

## Active Technologies
- Vanilla JavaScript (ES modules in existing Vite-served app) + Bootstrap (existing UI styling), Firebase (existing persistence/metrics), Playwright + Vitest (existing testing) (002-improve-lowres-quality)
- Firebase-backed profile persistence with existing localStorage fallback patterns (002-improve-lowres-quality)

- Vanilla JavaScript (ES modules in existing Vite-served app) + Bootstrap (existing UI styling system), Firebase (existing storage and sharing stack) (001-recommended-palette-settings)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test; npm run lint

## Code Style

Vanilla JavaScript (ES modules in existing Vite-served app): Follow standard conventions

## Recent Changes
- 002-improve-lowres-quality: Added Vanilla JavaScript (ES modules in existing Vite-served app) + Bootstrap (existing UI styling), Firebase (existing persistence/metrics), Playwright + Vitest (existing testing)

- 001-recommended-palette-settings: Added Vanilla JavaScript (ES modules in existing Vite-served app) + Bootstrap (existing UI styling system), Firebase (existing storage and sharing stack)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
